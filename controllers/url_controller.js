
const shorten_url = async (req, res, next) => {
    //TODO: implement shorten_url method in PR
};

const url_redirect = async (req, res, next) => {
    //TODO: implement redirect method in PR
};

export default {
    shorten_url,
    url_redirect
}